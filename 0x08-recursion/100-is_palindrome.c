#include "main.h"

/**
* is_palindrome - shows if a str is emkpty
* @s: the strr
* Return: 1 if emty else 0
*/
int is_palindrome(char *s)
{
	if (*s == '\0' || *s == 0)
		return (1);
	else
		return (0);
	return (0);
}
