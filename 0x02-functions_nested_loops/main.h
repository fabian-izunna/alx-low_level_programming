#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
* main - prints whether a number is positive or negative
* Return: 0 if exited properly non-zero otherwise
*/
int main(void)
{
	return (0);
}
int _putchar(char c)

{

		return (write(1, &c, 1));

}
